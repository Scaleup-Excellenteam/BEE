# Part B (semantic search) runtime dependencies.
# Part A (corpus, matching, CLI) needs none of these and runs on the
# standard library alone.
google-genai>=2.10.0
numpy>=2.0

# Local (offline) embeddings for semantic log search.
# No API key required; used instead of Gemini on that path.
sentence-transformers>=5.0
